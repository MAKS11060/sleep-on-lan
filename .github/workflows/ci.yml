name: CI

on:
  # push:
    # branches: [main]
    workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Clone repo
        uses: actions/checkout@v4

      - name: Build
        run: cargo build --verbose --target x86_64-pc-windows-msvc
